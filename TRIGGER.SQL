CREATE TRIGGER CONTROLLO_SALDO
BEFORE INSERT ON MOVIMENTO
DECLARE     SALDO   NUMBER;
BEGIN
    IF 1000 < :NEW.QUANTITA
    THEN 
        RAISE_APPLICATION_ERROR(-2061,'SALDO NON SUFFICIENTE');
    END IF;
END;